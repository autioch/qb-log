const width = require('./width');
const repeatString = require('repeat-string');

/**
 * String of spaces of the same width as a current time function.
 * @type {[type]}
 */
module.exports = repeatString(' ', width);
